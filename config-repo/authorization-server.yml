server:
  port: 9999
server.forward-headers-strategy: framework

mock:
  merchant.client.enabled: false

app.jwt.issuer-uri: http://localhost:9999
---
spring.config.activate.on-profile: docker

server.port: 80

app.jwt.issuer-uri: http://authorization-server:9999